     │ Bino Rehab 성능 및 품질 개선 계획                                           │
     │                                                                             │
     │ Context                                                                     │
     │                                                                             │
     │ 양안시 재활 분석 웹앱(Next.js 16 + MediaPipe)의 성능을 개선한다. 현재 주요  │
     │ 병목은 프레임당 3회 상태 업데이트로 인한 초당 ~90회 불필요한 리렌더링이며,  │
     │ 번들 크기, 타입 안전성, 테스트 부재 등의 품질 이슈도 함께 해결한다. 앱의    │
     │ 목적(실시간 홍채 추적 + 임상 지표 계산)과 UI는 그대로 유지한다.             │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 1: 리렌더링 제거 (CRITICAL — 체감 성능 최대 개선)                     │
     │                                                                             │
     │ 1A. Zustand 개별 셀렉터 적용                                                │
     │                                                                             │
     │ 파일: web/src/app/page.tsx (line 21)                                        │
     │                                                                             │
     │ 현재 useAnalysisStore() 전체 구독 → 개별 셀렉터로 분리:                     │
     │ const velocity = useAnalysisStore((s) => s.velocity);                       │
     │ const symmetry = useAnalysisStore((s) => s.symmetry);                       │
     │ const isRecording = useAnalysisStore((s) => s.isRecording);                 │
     │ // ...각각 독립 구독                                                        │
     │                                                                             │
     │ 1B. 차트/메트릭 컴포넌트 분리 + React.memo                                  │
     │                                                                             │
     │ 파일: web/src/app/page.tsx                                                  │
     │                                                                             │
     │ - VelocityChart: history만 구독하는 메모이즈드 컴포넌트                     │
     │ - RealtimeMetrics: velocity, symmetry만 구독                                │
     │ - ClinicalPanel: clinical만 구독                                            │
     │                                                                             │
     │ 1C. 상태 업데이트 배치화                                                    │
     │                                                                             │
     │ 파일: web/src/hooks/useAnalysisStore.ts, web/src/hooks/useBinocularLogic.ts │
     │                                                                             │
     │ 프레임당 3회 set() 호출 → 1회 updateFrame() 액션으로 통합:                  │
     │ // useAnalysisStore.ts에 추가                                               │
     │ updateFrame: (v, s, t, vel) => set((state) => ({                            │
     │   velocity: v,                                                              │
     │   symmetry: s,                                                              │
     │   history: [...state.history.slice(-100), { t, v: vel }],                   │
     │ })),                                                                        │
     │                                                                             │
     │ 1D. 차트 히스토리 업데이트 쓰로틀링                                         │
     │                                                                             │
     │ 히스토리를 매 프레임이 아닌 3프레임마다 갱신 (~10fps) — 사람 눈이 구분      │
     │ 불가한 수준에서 차트 리렌더 1/3로 절감.                                     │
     │                                                                             │
     │ 기대 효과: 리렌더 90회/초 → ~10회/초                                        │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 2: GC 압력 감소                                                       │
     │                                                                             │
     │ 2A. Ring Buffer로 히스토리 배열 교체                                        │
     │                                                                             │
     │ 새 파일: web/src/utils/RingBuffer.ts                                        │
     │ 수정: web/src/hooks/useAnalysisStore.ts                                     │
     │                                                                             │
     │ 현재 매 프레임 [...slice(-100), newItem] 배열 복사 → 고정 크기 순환 버퍼로  │
     │ 교체. toArray()는 차트 렌더 시에만 호출.                                    │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 3: 캔버스 최적화                                                      │
     │                                                                             │
     │ 3A. 바운딩 박스 랜드마크 축소                                               │
     │                                                                             │
     │ 파일: web/src/components/VideoAnalyzer.tsx (lines 211-217)                  │
     │                                                                             │
     │ 478개 전체 랜드마크 순회 → 얼굴 윤곽 36개 인덱스(FACE_OVAL_INDICES)만 사용. │
     │ 13배 반복 횟수 감소.                                                        │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 4: 번들 크기 최적화                                                   │
     │                                                                             │
     │ 4A. 미사용 의존성 제거                                                      │
     │                                                                             │
     │ 파일: web/package.json                                                      │
     │                                                                             │
     │                                                                             │
     │ ┌────────────────┬────────────┬──────────────────────────────────────┐      │
     │ │     패키지     │ 크기(gzip) │              사용 여부               │      │
     │ ├────────────────┼────────────┼──────────────────────────────────────┤      │
     │ │ framer-motion  │ ~63KB      │ 미사용                               │      │
     │ ├────────────────┼────────────┼──────────────────────────────────────┤      │
     │ │ react-webcam   │ ~3KB       │ 미사용 (native getUserMedia 사용 중) │      │
     │ ├────────────────┼────────────┼──────────────────────────────────────┤      │
     │ │ tailwind-merge │ ~5KB       │ 미사용                               │      │
     │ └────────────────┴────────────┴──────────────────────────────────────┘      │
     │                                                                             │
     │ npm uninstall framer-motion react-webcam tailwind-merge                     │
     │                                                                             │
     │ 기대 효과: ~70KB+ 번들 절감                                                 │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 5: 타입 안전성 강화                                                   │
     │                                                                             │
     │ 5A. getCentroid 파라미터 타입 지정                                          │
     │                                                                             │
     │ 파일: web/src/components/VideoAnalyzer.tsx (line 8)                         │
     │                                                                             │
     │ any[] → MediaPipe의 NormalizedLandmark[] 타입 적용                          │
     │                                                                             │
     │ 5B. Error catch 타입 개선                                                   │
     │                                                                             │
     │ catch (error: any) → catch (error: unknown) + instanceof Error 분기         │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 6: processFrame 안정성                                                │
     │                                                                             │
     │ 6A. isRecording을 ref로 전환                                                │
     │                                                                             │
     │ 파일: web/src/hooks/useBinocularLogic.ts (line 209)                         │
     │                                                                             │
     │ isRecording이 useCallback 의존성에 포함 → 녹화 토글 시 콜백 재생성 → RAF    │
     │ 루프 재시작. ref로 읽어 콜백 identity 안정화:                               │
     │ const isRecordingRef = useRef(isRecording);                                 │
     │ useEffect(() => { isRecordingRef.current = isRecording; }, [isRecording]);  │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 7: 디버그 UI 조건부 렌더링                                            │
     │                                                                             │
     │ 7A. 프로덕션에서 디버그 바 숨김                                             │
     │                                                                             │
     │ 파일: web/src/components/VideoAnalyzer.tsx (lines 319-321)                  │
     │                                                                             │
     │ process.env.NODE_ENV === 'development' 조건으로 래핑. setDebugInfo 호출도   │
     │ 동일 조건 적용.                                                             │
     │                                                                             │
     │ 7B. 프레임 카운터 바운딩                                                    │
     │                                                                             │
     │ frameCountRef.current++ → modulo 연산으로 오버플로 방지.                    │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 8: CSS 최적화                                                         │
     │                                                                             │
     │ 8A. Backdrop-filter 블러 경량화                                             │
     │                                                                             │
     │ 파일: web/src/app/globals.css (line 57)                                     │
     │                                                                             │
     │ blur(12px) → blur(8px) (시각적 차이 미미, GPU 부하 감소). 프로파일링 결과에 │
     │ 따라 선택 적용.                                                             │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 9: 테스트 추가                                                        │
     │                                                                             │
     │ 9A. 임상 계산 로직 분리 + 단위 테스트                                       │
     │                                                                             │
     │ 새 파일: web/src/utils/clinicalCalculations.ts,                             │
     │ web/src/__tests__/clinicalCalculations.test.ts                              │
     │                                                                             │
     │ useBinocularLogic.ts의 computeClinicalMetrics()에서 순수 계산 함수를        │
     │ 분리하고 Vitest로 테스트:                                                   │
     │ - mmToPrismDiopter 변환                                                     │
     │ - Phoria 계산 (원거리/근거리)                                               │
     │ - PRC/NRC 범위                                                              │
     │ - AC/A ratio                                                                │
     │ - NPC, 최대조절력                                                           │
     │ - 엣지 케이스 (샘플 < 10, 속도 0, PD 0)                                     │
     │                                                                             │
     │ npm install -D vitest                                                       │
     │                                                                             │
     │ ---                                                                         │
     │ Phase 10: Web Worker (향후 — 별도 스프린트)                                 │
     │                                                                             │
     │ 10A. MediaPipe를 Web Worker로 이전                                          │
     │                                                                             │
     │ detectForVideo()를 메인 스레드에서 분리. OffscreenCanvas + ImageBitmap 활용.│
     │  Safari 미지원이므로 CPU fallback 필요. AGENTS.md 권장사항이며 60fps 달성의 │
     │ 핵심.                                                                       │
     │                                                                             │
     │ ▎ 이 단계는 Phase 1~6 검증 후 진행.                                         │
     │                                                                             │
     │ ---                                                                         │
     │ 수정 대상 파일 요약                                                         │
     │                                                                             │
     │                                                                             │
     │ ┌───────────────────────────────────────────────────────┬───────────────────│
     │ ─┐                                                                          │
     │ │                         파일                          │     관련 Phase    │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/app/page.tsx                                  │ 1A, 1B            │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/hooks/useAnalysisStore.ts                     │ 1C, 1D, 2A        │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/hooks/useBinocularLogic.ts                    │ 1C, 6A, 9A        │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/components/VideoAnalyzer.tsx                  │ 3A, 5A, 5B, 7A, 7B│
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/package.json                                      │ 4A, 9A            │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/app/globals.css                               │ 8A                │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/utils/RingBuffer.ts (신규)                    │ 2A                │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/utils/clinicalCalculations.ts (신규)          │ 9A                │
     │  │                                                                          │
     │ ├───────────────────────────────────────────────────────┼───────────────────│
     │ ─┤                                                                          │
     │ │ web/src/__tests__/clinicalCalculations.test.ts (신규) │ 9A                │
     │  │                                                                          │
     │ └───────────────────────────────────────────────────────┴───────────────────│
     │ ─┘                                                                          │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │                                                                             │
     │ ---                                                                         │
     │ 검증 방법                                                                   │
     │                                                                             │
     │ 1. 리렌더 확인: React DevTools Profiler로 프레임당 리렌더 컴포넌트 수 비교  │
     │ (Before/After)                                                              │
     │ 2. 번들 크기: npm run build 후 .next/static 크기 비교                       │
     │ 3. 프레임 성능: Chrome DevTools Performance 탭에서 30초 녹화 → 프레임 드롭률│
     │  비교                                                                       │
     │ 4. 기능 동작: 카메라 연결 → 분석 시작/중지 → 임상 지표 계산 결과 이전과     │
     │ 동일한지 확인                                                               │
     │ 5. 테스트: npx vitest run 으로 임상 계산 테스트 통과 확인    