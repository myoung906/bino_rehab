{
  "name": "bino-rehab-desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "preview": "tauri dev --build-only"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
