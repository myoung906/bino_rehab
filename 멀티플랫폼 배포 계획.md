# 양안시 분석 애플리케이션 멀티플랫폼 배포 구현 계획

## 목표

Next.js 기반 웹 애플리케이션을 Windows, Android, iOS용 독립 실행 파일로 확장 배포

## 핵심 전략

- **코드 재사용 극대화**: 기존 Next.js static export를 모든 플랫폼에서 공유
- **균형잡힌 기술 스택**: Tauri(Windows) + Capacitor(Android/iOS)
- **웹 버전 유지**: 기존 GitHub Pages 배포 계속 운영

---

## 선택된 기술 스택

| 플랫폼 | 프레임워크 | 번들 크기 | 개발 기간 | 코드 재사용률 |
|--------|-----------|-----------|-----------|---------------|
| Windows | Tauri 2.x | 3-5MB | 1주 | 100% |
| Android | Capacitor 6.x | 15-20MB | 1주 | 100% |
| iOS | Capacitor 6.x | 15-20MB | 1주 | 100% |
| Web | GitHub Pages (유지) | 1.5MB | - | - |

### 선택 이유

- MediaPipe WASM 버전을 모든 플랫폼에서 재사용 가능
- 네이티브 재작성 불필요 (React Native 대비 개발 시간 70% 절감)
- Tauri: Electron 대비 10배 작은 번들, 높은 성능
- Capacitor: 웹 기술로 모바일 네이티브 기능 접근

---

## 프로젝트 구조 (재구성)

```
bino_rehab/
├── web/                          # 공통 코드베이스 (기존 유지)
│   ├── src/
│   ├── package.json              # 수정: 빌드 스크립트 추가
│   ├── next.config.ts            # 수정: basePath 환경변수 분기
│   └── out/                      # static export 결과 (래퍼 소스)
│
├── desktop/                      # 신규: Tauri 래퍼
│   ├── src-tauri/
│   │   ├── tauri.conf.json       # Tauri 설정
│   │   ├── Cargo.toml            # Rust 의존성
│   │   └── src/main.rs
│   └── package.json
│
├── mobile/                       # 신규: Capacitor 래퍼
│   ├── android/                  # Android Studio 프로젝트
│   ├── ios/                      # Xcode 프로젝트
│   ├── capacitor.config.ts       # Capacitor 설정
│   └── package.json
│
└── scripts/                      # 신규: 빌드 자동화
    ├── build-all.sh
    ├── build-desktop.sh
    └── build-mobile.sh
```

---

## 구현 단계 (4주)

### Week 1: 기반 구축

#### 1.1 프로젝트 구조 생성

```bash
mkdir -p desktop mobile scripts
```

#### 1.2 web/next.config.ts 수정

```typescript
const buildTarget = process.env.BUILD_TARGET || 'gh-pages';
const isProd = process.env.NODE_ENV === "production";

const nextConfig: NextConfig = {
  output: "export",
  basePath: buildTarget === 'gh-pages' && isProd ? "/bino_rehab" : "",
  images: { unoptimized: true },
};
```

#### 1.3 web/package.json 스크립트 추가

```json
{
  "scripts": {
    "build:gh-pages": "BUILD_TARGET=gh-pages next build",
    "build:standalone": "BUILD_TARGET=standalone next build"
  }
}
```

#### 1.4 .gitignore 업데이트

```gitignore
desktop/src-tauri/target/
mobile/android/build/
mobile/ios/build/
*.apk
*.ipa
*.msi
```

---

### Week 2: Windows (Tauri)

#### 2.1 환경 설정

```bash
# Rust 설치
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Tauri 프로젝트 초기화
cd desktop
npm init -y
npm install -D @tauri-apps/cli
npx tauri init
```

#### 2.2 desktop/src-tauri/tauri.conf.json 작성

```json
{
  "build": {
    "beforeBuildCommand": "cd ../web && BUILD_TARGET=standalone npm run build",
    "frontendDist": "../web/out"
  },
  "app": {
    "windows": [
      {
        "title": "Binocular Vision Rehab",
        "width": 1280,
        "height": 800
      }
    ],
    "security": {
      "csp": "default-src 'self'; script-src 'self' 'unsafe-inline' 'wasm-unsafe-eval'; connect-src 'self' https://cdn.jsdelivr.net https://storage.googleapis.com"
    }
  },
  "bundle": {
    "targets": ["msi", "nsis"],
    "icon": ["icons/icon.ico", "icons/icon.png"]
  }
}
```

#### 2.3 아이콘 생성

```bash
# 1024x1024 PNG를 ICO로 변환
# https://icoconvert.com/ 사용 또는 ImageMagick
```

#### 2.4 빌드 테스트

```bash
npm run tauri build
# 결과: desktop/src-tauri/target/release/bundle/msi/*.msi
```

---

### Week 3: Android (Capacitor)

#### 3.1 환경 설정

```bash
# Android Studio 설치 확인
# JDK 17 설치 확인
cd mobile
npm init -y
npm install @capacitor/core @capacitor/cli @capacitor/android
```

#### 3.2 Capacitor 초기화

```bash
npx cap init
# App name: Binocular Vision Rehab
# App ID: com.binorehab.app
# Web asset directory: ../web/out

npx cap add android
```

#### 3.3 mobile/capacitor.config.ts 작성

```typescript
const config: CapacitorConfig = {
  appId: 'com.binorehab.app',
  appName: 'Binocular Vision Rehab',
  webDir: '../web/out',
  server: {
    androidScheme: 'https',  // WASM 로딩 필수
    cleartext: false
  }
};
```

#### 3.4 AndroidManifest.xml 수정

```xml
<!-- mobile/android/app/src/main/AndroidManifest.xml -->
<uses-permission android:name="android.permission.CAMERA" />
<uses-feature android:name="android.hardware.camera" android:required="true" />
```

#### 3.5 빌드 및 서명

```bash
# 키스토어 생성 (1회만)
keytool -genkey -v -keystore binorehab-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias binorehab

# build.gradle 서명 설정 추가
# mobile/android/app/build.gradle

# 빌드
cd mobile
npm run build  # web 빌드 + sync
cd android
./gradlew assembleRelease
```

---

### Week 4: iOS (Capacitor) + 통합 테스트

#### 4.1 iOS 추가 (macOS 필수)

```bash
cd mobile
npm install @capacitor/ios
npx cap add ios
```

#### 4.2 Info.plist 수정

```xml
<!-- mobile/ios/App/App/Info.plist -->
<key>NSCameraUsageDescription</key>
<string>Eye tracking analysis requires camera access</string>
```

#### 4.3 Xcode 빌드

```bash
npm run open:ios
# Xcode에서 Signing & Capabilities 설정
# Product > Archive > Distribute App
```

#### 4.4 통합 테스트

- Windows: MSI 설치 후 카메라 동작 확인
- Android: 에뮬레이터/실제 기기에서 APK 테스트
- iOS: 시뮬레이터에서 테스트

---

## 수정/추가 파일 목록

### 수정 필요 (우선순위순)

| 순서 | 파일 | 작업 내용 | 추가 라인 |
|------|------|----------|----------|
| 1 | `web/next.config.ts` | BUILD_TARGET 환경변수 기반 basePath 분기 | 3줄 |
| 2 | `web/package.json` | build:standalone 스크립트 추가 | 1줄 |
| 3 | `.gitignore` | Tauri/Capacitor 빌드 산출물 제외 | 5줄 |
| 4 | `README.md` | 멀티플랫폼 다운로드 링크 섹션 추가 | - |

### 신규 생성 (핵심 파일만)

| 순서 | 파일 | 설명 |
|------|------|------|
| 1 | `desktop/src-tauri/tauri.conf.json` | Tauri 핵심 설정 |
| 2 | `desktop/src-tauri/Cargo.toml` | Rust 의존성 |
| 3 | `desktop/src-tauri/src/main.rs` | Rust 진입점 (10줄) |
| 4 | `mobile/capacitor.config.ts` | Capacitor 핵심 설정 |
| 5 | `scripts/build-all.sh` | 통합 빌드 자동화 |

---

## 빌드 명령어 요약

### 개발 모드

```bash
# Web
cd web && npm run dev

# Windows (HMR 지원)
cd desktop && npm run tauri dev

# Android
cd mobile && npm run build && npm run open:android

# iOS
cd mobile && npm run build:ios && npm run open:ios
```

### 프로덕션 빌드

```bash
# 전체 플랫폼 일괄 빌드
bash scripts/build-all.sh

# 또는 개별 빌드
cd web && BUILD_TARGET=standalone npm run build      # Web
cd desktop && npm run tauri build                     # Windows MSI/NSIS
cd mobile/android && ./gradlew assembleRelease        # Android APK
# iOS는 Xcode에서 Archive
```

### 빌드 결과물 위치

```
desktop/src-tauri/target/release/bundle/msi/*.msi
desktop/src-tauri/target/release/bundle/nsis/*.exe
mobile/android/app/build/outputs/apk/release/*.apk
# iOS IPA는 Xcode Organizer에서 추출
```

---

## 주요 이슈 해결 방법

### MediaPipe WASM 로딩 실패

| 항목 | 내용 |
|------|------|
| **증상** | CSP 정책 위반 |
| **해결** | `tauri.conf.json`에서 `'wasm-unsafe-eval'` 추가 |

### 모바일 성능 저하 (30fps 미만)

| 항목 | 내용 |
|------|------|
| **증상** | 프레임 드롭 |
| **해결** | 비디오 해상도 640x480으로 제한, `minFaceDetectionConfidence: 0.3`으로 완화 |

### Android APK 크기 > 100MB

| 항목 | 내용 |
|------|------|
| **증상** | Play Store 업로드 거부 |
| **해결** | AAB(App Bundle) 사용: `./gradlew bundleRelease`, ProGuard 활성화: `minifyEnabled true` |

### iOS 카메라 권한 거부

| 항목 | 내용 |
|------|------|
| **증상** | NotAllowedError |
| **해결** | `Info.plist`에 `NSCameraUsageDescription` 추가 |

---

## 검증 체크리스트

### Windows

- [ ] MSI 설치 성공
- [ ] 카메라 접근 권한 요청 다이얼로그 표시
- [ ] MediaPipe 얼굴 감지 정상 동작 (60fps)
- [ ] 임상 메트릭 계산 정확성
- [ ] 앱 크기 < 10MB

### Android

- [ ] APK 설치 성공 (사이드로드)
- [ ] 카메라 권한 런타임 요청
- [ ] MediaPipe 얼굴 감지 동작 (30fps)
- [ ] 터치 UI 반응성
- [ ] APK 크기 < 50MB

### iOS

- [ ] IPA 설치 성공 (TestFlight)
- [ ] 카메라 권한 설명 표시
- [ ] MediaPipe 동작 확인
- [ ] Safe Area 레이아웃 정상

### 통합

- [ ] 웹 버전 기존 기능 유지
- [ ] 모든 플랫폼에서 동일한 분석 결과
- [ ] 반응형 레이아웃 정상 동작

---

## 배포 옵션

### 즉시 배포 가능 (앱 스토어 없이)

| 플랫폼 | 방법 |
|--------|------|
| Windows | 웹사이트에 MSI/NSIS 업로드 |
| Android | APK 직접 다운로드 (사이드로드) |
| iOS | TestFlight 베타 배포 |

### 앱 스토어 배포 (추후)

| 스토어 | 형식 | 비용 |
|--------|------|------|
| Google Play Store | AAB 업로드 | $25 (일회) |
| Apple App Store | IPA 업로드 | $99/년 |
| Microsoft Store | MSIX 업로드 | 선택 (Tauri 2.0 지원) |

---

## 예상 결과

### 개발 기간: 4주

| 주차 | 작업 내용 |
|------|----------|
| Week 1 | 프로젝트 구조 재구성 |
| Week 2 | Windows (Tauri) |
| Week 3 | Android (Capacitor) |
| Week 4 | iOS (Capacitor) + 테스트 |

### 번들 크기

| 플랫폼 | 크기 |
|--------|------|
| Windows | 3-5MB (MSI) |
| Android | 15-20MB (APK) |
| iOS | 15-20MB (IPA) |

### 코드 변경

| 항목 | 수량 |
|------|------|
| 기존 파일 수정 | 4개 |
| 신규 파일 생성 | ~15개 |
| 총 코드 추가량 | ~500줄 (대부분 설정 파일) |

### 핵심 이점

- [x] 기존 React/TypeScript 코드 100% 재사용
- [x] MediaPipe WASM 모든 플랫폼 공유
- [x] 웹 버전 계속 운영 가능
- [x] 네이티브 재작성 불필요 (개발 시간 절감)
- [x] 플랫폼별 최적화 가능 (카메라 권한, 푸시 알림 등)
